
<MudListItem Class="py-0">
    <div class="d-flex align-center">
        <MudTooltip Text="@Label" Placement="Placement.Top">
            <MudText Typo="Typo.h6"
                     Class="d-inline-block type"
                     Color="Color.Info">
                @Label[..1]
            </MudText>
        </MudTooltip>
        <RewardThumbs Value="Model.Value" />
        <MudTooltip Text="@Model.Tooltip" Placement="Placement.Top">
            @Model.Reward
        </MudTooltip>
    </div>
</MudListItem>

@code {
    [Parameter] public RewardModel Model { get; set; }
    [Parameter] public string Label { get; set; }
}
